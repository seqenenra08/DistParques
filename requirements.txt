# Dependencias del proyecto Parqués - Backend Python

# Core dependencies (Fase 2+)
# websockets==12.0  # Para comunicación en tiempo real (opcional)
# aiohttp==3.9.0    # Cliente/servidor HTTP asíncrono (opcional)
# python-socketio==5.10.0  # Para WebSocket con fallback (opcional)

# Utilities
python-dotenv==1.0.0  # Variables de entorno

# Data validation (recomendado para protocolo JSON)
pydantic==2.5.0  # Validación de datos

# Database (para fase futura)
# sqlalchemy==2.0.23  # ORM
# asyncpg==0.29.0     # PostgreSQL async

# Testing (Fase 4+)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Nota: Fase 1-2 usa principalmente biblioteca estándar + pydantic para validación
